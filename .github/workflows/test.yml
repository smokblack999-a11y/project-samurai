name: test
on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      # The repository still contains an unrelated legacy api-lifecycle tree
      # with missing internal packages. Test the SamuraiOS engine explicitly
      # until that legacy tree is repaired or removed.
      - run: go test ./cmd/samurai ./internal/audit
      - run: go vet ./cmd/samurai ./internal/audit
      - run: go build ./cmd/samurai
      - run: go run ./cmd/samurai audit --format json target.json
      - run: go run ./cmd/samurai audit --format sarif target.json > samurai-clean.sarif
      - name: Verify failing fixture trips the gate
        shell: bash
        run: |
          set +e
          go run ./cmd/samurai audit --format json --fail-on high target-bad.json
          rc=$?
          set -e
          test "$rc" -eq 1
      - run: go run ./cmd/samurai audit --format sarif target-bad.json > samurai-bad.sarif
      - uses: actions/upload-artifact@v4
        with:
          name: samurai-sarif-fixtures
          path: '*.sarif'

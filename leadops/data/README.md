# Evaluation dataset

Use anonymized real Telegram messages for the commercial benchmark.

Required fields: id, text, intent, score, action.

Do not commit API keys, usernames, phone numbers, payment data, or unnecessary personal data. Keep production exports outside Git unless explicitly sanitized.
